services:
  discord-notify:
    build: .
    container_name: discord-notify
    restart: unless-stopped
    env_file: .env
    volumes:
      - ./data:/app/data
    networks:
      - discord-notify

networks:
  discord-notify:
    name: discord-notify
